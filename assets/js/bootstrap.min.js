  var pass = ''; 
    $('#password').keyup(function(e) {
      var strongRegex = new RegExp("^(?=.{8,})(?=.*[A-Z])(?=.*[a-z])(?=.*[0-9])(?=.*\\W).*$", "g");
      var mediumRegex = new RegExp("^(?=.{7,})(((?=.*[A-Z])(?=.*[a-z]))|((?=.*[A-Z])(?=.*[0-9]))|((?=.*[a-z])(?=.*[0-9]))).*$", "g");
      var enoughRegex = new RegExp("(?=.{6,}).*", "g");
   
      if (false == enoughRegex.test($(this).val())) {
              $('#passstrength').html('');
              $('#passstrength').width('0%');
               pass = '0'; 
      } else if (strongRegex.test($(this).val())) {
              $('#passstrength').width('100%');
              $('#passstrength').html('Fuerte');
               pass = '100'; 
      } else if (mediumRegex.test($(this).val())) {
              $('#passstrength').width('50%');
              $('#passstrength').html('Media');
               pass = '50'; 
      } else {
              $('#passstrength').width('25%');
              $('#passstrength').html('Debil');
               pass = '25'; 
      }
  });




 $(document).ready(function(e) {
  $("#formAuthentication").on('submit', function(e) {

    e.preventDefault();

    let formData = new FormData(this);


    if ($('#password-2').val() == '' || $('#password').val() == '') {
      toast('error', 'Hay campos vacios' )
      return;
    }

    if ($('#password-2').val() != $('#password').val()) {
      toast('error', 'Las contraseñas no coinciden' )
      return;
    }


    if (pass != '100') {
      toast("error", "La contraseña debe tener al menos una (1) mayúscula, un (1) numero y un (1) carácter especial.");
      return;
    }
/*
    switch (pass) {
      case '0':
            toast('error', 'La contraseña no es segura' )
      return;
        break;
        case '25':
            toast('error', 'La contraseña no es segura' )
      return;
    }*/

    formData.append('u', u);
    formData.append('t', t);


    $.ajax({
      type: 'POST',
      url: 'back/users-activate.php',
      data: formData,
      contentType: false,
      cache: false,
      processData: false,
      success: function(msg) {
       console.log(msg)
       
        if (msg.trim() == 'acivado') {
          toast('success', 'Correcto' )
          window.location.href='index';
        } else if (msg.trim() == 'error_t') {
          toast('error', 'error en el token' )

        }else if ( msg.trim() == 'error_n') {
          toast('error', 'sin peticion' )
       
        }else if(msg.trim() == 'error_i'){
          toast('error', 'Error 985' )
        }





      }
    });

  });

});